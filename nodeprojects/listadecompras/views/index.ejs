<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Não esquecer de comprar os seguintes itens:</p>
    <table style="width:50%">
      <thead>
        <tr style="background-color: #CCC">
          <td>Nome</td>
          <td style="width:20%">Qtd.</td>
          <td>Excluir</td>
        </tr>
      </thead>
      <tbody>
        <% if(!docs || docs.length == 0) { %>
        <tr>
          <td colspan="2">Nenhum item cadastrado.</td>
        </tr>
        <% } else {
          docs.forEach(function(item){ %>
            <tr>
              <td><a href="items/new/<%= item._id %>"><%= item.nome %></a></td>
              <td style="width:20%"><%= item.quantidade %></td>
              <td><a href="items/delete/<%= item._id %>" onClick="return confirm('Tem certeza?')">Excluir</a></a></td>
            </tr>
          <% });
          }
        %>
      </tbody>
      <tfoot>
        <tr>
          <td colspan="3">
            <a href="./items/new">Cadastrar Novo</a>
          </td>
        </tr>
      </tfoot>
      </table>
  </body>
</html>
